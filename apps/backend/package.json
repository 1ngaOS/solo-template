{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "description": "Rust backend API server",
  "scripts": {
    "dev": "cargo watch -x run",
    "build": "cargo build --release",
    "test": "cargo test",
    "lint": "cargo clippy",
    "format": "cargo fmt",
    "check": "cargo check"
  },
  "engines": {
    "rust": ">=1.70.0"
  }
}

